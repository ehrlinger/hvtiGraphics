# Function-to-recipe map

Use this map when you know the function name but not the recipe. It answers the
question that arrives when you are reading someone else's script: *what is
`hv_atrisk_compose()`, and where in this book is it worked through?*

The map is a shortcut back to a reproducible example, not a substitute for one.
It keeps a function lookup from becoming an invitation to reconstruct a workflow
from its argument list.

## How to read it

**Role** says what the function is for in one line. **Recipe** links to the
chapter that works it through. **Coverage** distinguishes a function this book
demonstrates from one it names but leaves to the package reference.

The table is generated rather than typed. The function lists are an explicit
contract, and the chapter column comes from scanning the book's own sources, so
the two can disagree and say so. If a function is added to the contract and no
chapter picks it up, its coverage flips to *Package reference* the next time the
map is built, without anyone remembering to update a list.

That build is `dev/build-function-map.R`, and it runs outside the render. This
chapter is the one place in the book whose output depends on *other* chapters,
and Quarto's `freeze: auto` re-executes a chapter only when its own source
changes. So while the map was computed here, every edit anywhere else in the
book quietly invalidated it and nothing failed. The script writes the fragment
below along with a manifest of the files it read, and CI rebuilds that manifest
and fails when it no longer matches. Run it after changing any chapter:

```bash
Rscript dev/build-function-map.R
```

{{< include _generated/function-map.md >}}

## What the map does not cover

Lower-level helpers, S3 methods reached through `plot()` or `print()`, and
compatibility shims kept for older scripts are all deliberately absent. So are
the deprecated theme aliases: `theme_hv_manuscript()` and its siblings are the
current names, and the older `hv_theme_*()` and `theme_man()` forms still work
but should not be written into new code.

If you are looking for a function that is not here, it is either one of those or
it is new since this edition. `hvtiR::status()` will tell you which.
